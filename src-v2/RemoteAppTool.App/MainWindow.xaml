<Window x:Class="RemoteAppTool.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="RemoteApp Tool v2" Height="600" Width="900"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" DockPanel.Dock="Top" Height="60" VerticalAlignment="Top" materialDesign:ElevationAssist.Elevation="Dp4">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ServerNetwork" Width="28" Height="28" VerticalAlignment="Center" />
                <TextBlock Margin="16 0 0 0" VerticalAlignment="Center" FontSize="22" FontWeight="SemiBold" Text="RemoteApp Tool Manager" />
            </StackPanel>
        </materialDesign:ColorZone>

        <Grid Margin="16, 80, 16, 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,16,0" Padding="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Content="RemoteApps" HorizontalAlignment="Stretch" Margin="0,0,0,8" />
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Connections" HorizontalAlignment="Stretch" Margin="0,0,0,8" />
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Content="RDP Options" HorizontalAlignment="Stretch" Margin="0,0,0,8" />
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Settings" HorizontalAlignment="Stretch" Margin="0,0,0,8" />
                </StackPanel>
            </materialDesign:Card>

            <!-- Main Content Area -->
            <materialDesign:Card Grid.Column="1" Padding="24" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="Welcome to the Modernized RemoteApp Tool" Margin="0,0,0,16" />
                    <TextBlock Style="{StaticResource MaterialDesignBody1TextBlock}" Text="Manage your hosted applications, generate RDP files, and create MSI installers with ease." TextWrapping="Wrap" />
                    
                    <StackPanel Orientation="Horizontal" Margin="0,32,0,0">
                        <Button Style="{StaticResource MaterialDesignRaisedButton}" Width="200" Content="CREATE NEW REMOTEAPP" />
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}" Width="200" Margin="16,0,0,0" Content="OPEN EXISTING" />
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>
