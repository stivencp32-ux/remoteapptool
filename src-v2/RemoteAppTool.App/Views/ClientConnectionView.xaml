<UserControl x:Class="RemoteAppTool.App.Views.ClientConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" Text="Client Connection Options" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="Configure default settings applied to new RDP files and MSIs." Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,0,0,24" />

                <!-- General -->
                <TextBlock Text="Server Configuration" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" FontWeight="SemiBold" Margin="0,0,0,16" />
                <TextBox x:Name="TxtServerAddress" materialDesign:HintAssist.Hint="Server Address or IP" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16"/>
                <TextBox x:Name="TxtServerPort" materialDesign:HintAssist.Hint="Server Port (Default 3389)" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,24"/>

                <Separator Margin="0,8,0,24" />

                <!-- RD Gateway -->
                <TextBlock Text="RD Gateway" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" FontWeight="SemiBold" Margin="0,0,0,16" />
                <TextBox x:Name="TxtGateway" materialDesign:HintAssist.Hint="RD Gateway Hostname" Style="{DynamicResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16"/>
                
                <Separator Margin="0,8,0,24" />
                
                <!-- Display & Local Resources -->
                <TextBlock Text="Local Resources &amp; Connection" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" FontWeight="SemiBold" Margin="0,0,0,16" />
                <CheckBox x:Name="ChkAdmin" Content="Connect as Administrator (Console Session)" Margin="0,0,0,8"/>
                <CheckBox x:Name="ChkClipboard" Content="Enable Clipboard redirection" Margin="0,0,0,8" IsChecked="True"/>
                <CheckBox x:Name="ChkPrinters" Content="Enable Printer redirection" Margin="0,0,0,8" IsChecked="True"/>
                <CheckBox x:Name="ChkDrives" Content="Enable Drive redirection" Margin="0,0,0,24"/>

                <Separator Margin="0,8,0,24" />

                <!-- Advanced Options -->
                <TextBlock Text="Advanced Options" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" FontWeight="SemiBold" Margin="0,0,0,16" />
                <TextBlock Text="Add any raw RDP strings here (one per line). These will be appended exactly as written to the generated files." Foreground="{DynamicResource MaterialDesignBodyLight}" TextWrapping="Wrap" Margin="0,0,0,8" />
                <TextBox x:Name="TxtAdvancedOptions" materialDesign:HintAssist.Hint="e.g. videoplaybackmode:i:1" Style="{DynamicResource MaterialDesignOutlinedTextBox}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="100" Margin="0,0,0,8"/>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{DynamicResource MaterialDesignRaisedButton}" Content="SAVE DEFAULTS" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
