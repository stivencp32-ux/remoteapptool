<UserControl x:Class="RemoteAppTool.App.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <materialDesign:PackIcon Kind="ServerNetwork" Width="96" Height="96" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,24"/>
            <TextBlock Text="RemoteApp Tool v2" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Modernized Edition" FontSize="18" Foreground="{DynamicResource MaterialDesignBodyLight}" HorizontalAlignment="Center" Margin="0,0,0,32"/>

            <TextBlock Text="A tool to create and manage RemoteApps seamlessly on Windows." HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Version 2.0.0 (WPF / .NET 8)" HorizontalAlignment="Center" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>
    </Grid>
</UserControl>
